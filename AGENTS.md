# AGENTS.md — agentic-memory-stack

The canonical, model-agnostic agent guide for this repo is **`CLAUDE.md`** (one source, shared by Claude Code, Codex, and Hermes — so it can't drift). It is imported below; if your tool doesn't follow the import, **read `CLAUDE.md` directly now** — it tells you what this project is, how to work in it, and where things live.

@CLAUDE.md
