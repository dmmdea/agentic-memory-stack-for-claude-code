# mem0-server runtime dependencies
# v0.17 F.2.4: Starlette pinned >= 1.0.1 (CVE remediation).
# v0.29.1: Starlette >= 1.3.1 (CVE-2026-54282 fixed in 1.3.0, CVE-2026-54283 in 1.3.1;
#          FastAPI request path) and cryptography >= 48.0.1 (GHSA-537c-gmf6-5ccf).
#          cryptography is capped < 49 so a fresh install reproduces the as-tested
#          48.x major (verified set: starlette 1.3.1, cryptography 48.0.1 — see
#          VERSIONS.md). Bump the cap via UPGRADE.md when moving to cryptography 49.x.
#          NOTE: this file documents the floors; the installer's pip lines in
#          install/1-wsl-services.sh are what actually enforce them (it does not
#          `pip install -r` this file).
fastapi>=0.110.0
starlette>=1.3.1
cryptography>=48.0.1,<49
uvicorn>=0.27.0
httpx>=0.27.0
pydantic>=2.6.0
qdrant-client>=1.9.0
mem0ai>=2.0.4
anyio>=4.3.0

# Dev / audit tools
# pip-audit installed in venv 2026-06-11 (v0.18 Phase G); gated by Test-MemoryStack INVARIANTS
# pip-audit clean as of 2026-06-11 (v0.18 Phase G)
pip-audit
